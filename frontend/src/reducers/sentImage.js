//Todo(perry): Clean this up
initialState = {
  sentFile: null
}

const sent = (state = initialState, action) => {
  switch (action.type) {
    case 'SENT_IMAGE':
      return {
        ...state,
        sentFile: 
      }
  }
}